version: '3'
services:
  rebounder-chain-frontend:
    build:
      context: ../rebounder-chain-frontend
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    volumes:
      - /app/node_modules
      - .:/app
  rebounder-chain-backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
        - "8099:8099"
